<UserControl x:Class="lab4quiz.Views.QuizSolverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lab4quiz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Rozwiązywanie quizu" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBlock Text="Czas:"/>
            <TextBlock Text="{Binding TimerDisplay}" FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>
            <Button Content="Wczytaj quiz" Command="{Binding LoadQuizCommand}" Margin="0 0 0 10"/>
            <Button Content="Start" Command="{Binding StartQuizCommand}" Margin="0 0 0 10"/>
            <ItemsControl ItemsSource="{Binding Questions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0 10 0 10">
                            <TextBlock Text="{Binding Text}" FontWeight="Bold"/>
                            <ItemsControl ItemsSource="{Binding Answers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Text}" IsChecked="{Binding IsSelected}" Margin="10 2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Content="Zakończ quiz" Command="{Binding EndQuizCommand}" Background="LightCoral" Margin="0 20 0 0"/>
        </StackPanel>
    </Grid>
</UserControl>
