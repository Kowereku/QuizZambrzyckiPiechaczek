<UserControl x:Class="lab4quiz.Views.QuizEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lab4quiz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Nazwa quizu:"/>
            <TextBox Text="{Binding QuizTitle}" Margin="0,0,0,10"/>

            <TextBlock Text="Treść pytania:"/>
            <TextBox Text="{Binding NewQuestionText}" Margin="0,0,0,5"/>

            <TextBlock Text="Odpowiedzi (zaznacz poprawną):"/>
            <ItemsControl ItemsSource="{Binding NewAnswers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton IsChecked="{Binding IsCorrect}" GroupName="Answers" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding Text}" Width="200"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="Dodaj pytanie" Command="{Binding AddQuestionCommand}" Margin="0,10,0,0"/>
            <Button Content="Zapisz quiz" Command="{Binding SaveQuizCommand}" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
